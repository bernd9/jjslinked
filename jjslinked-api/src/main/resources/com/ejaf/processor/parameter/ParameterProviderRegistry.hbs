package com.ejaf.generated;

import {{providerSuperClassPackageName}}.{{providerSuperClassSimpleName}};
import com.ejaf.processor.annotations.*;

public class ParameterProviderRegistry {

    private static Map<> PROVIDER_MAPPING = new HashMap<Class<? extends Annotation>,Class<? extends ParameterProvider<?>>();
    private static Set<> PROVIDERS = new HashSet<Class<? extends ParameterProvider<?>>();

    static {
        {{#annotationMapping}}
        PROVIDER_MAPPING.put({{annotation}}.class, {{provider}}.class);
        PROVIDERS.add({{provider}}.class);
        {{/annotationMapping}}
    }

    public static Class<? extends ParameterProvider> getParameterProvider(Class<? extends Annotation> annotation) {
        return PROVIDER_MAPPING.get(annotation);
    }

}