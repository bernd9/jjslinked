class ChatService$JJSLinked extends MessageService {

    private final SenderMethod0 senderMethod0;

    sendMessage(LocalDateTime date, String message, String userId) {
        senderMethod0(date, message, userId);
    }
}